name: lyapunov-geo
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - numpy>=1.24
  - scipy>=1.10
  - pandas>=2.0
  - pytest>=7.4
  - ruff>=0.5
  - pre-commit>=3.5
  - pip
  - pip:
      - -e .
